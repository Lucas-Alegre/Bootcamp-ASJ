let numero = parseInt(prompt("Ingrese un numero"));
let suma = 0;

while (numero >= 0) {
  numero = parseInt(prompt("Ingrese un numero"));
  suma++;
}
alert("Usted  ingreso " + suma + " numeros positivos");
